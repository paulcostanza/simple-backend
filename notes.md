package.json: keeps track of all dependencies
node_modules: all the bullshit that the dependencies contains

middleware: somthing that goes into our APIs that can do a bunch of stuff. For this project we use <code>express</code>
- validating the cookie